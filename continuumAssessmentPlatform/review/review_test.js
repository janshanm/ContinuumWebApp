'use strict';

describe('continuumAssessmentPlatform.review module', function() {

  beforeEach(module('continuumAssessmentPlatform.review'));

  describe('review controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var reviewCtrl = $controller('ReviewCtrl');
      expect(reviewCtrl).toBeDefined();
    }));

  });
});